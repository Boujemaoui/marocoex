
import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Asegúrate de que la ruta sea correcta

// Se monta la aplicación en el contenedor 'root'
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
